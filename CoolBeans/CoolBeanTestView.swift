//
//  CoolBeanTestView.swift
//  CoolBeans
//
//  Created by Bektur Mamytov on 8/10/22.
// This view has been created for testing a github remote repositories.

import SwiftUI

struct CoolBeanTestView: View {
    var body: some View {
        Text("Hello, Git!")
        Text("Adding a text to test push by xcode")
        Text("Added this line of code to test branching")
        Text("Added this line of code to add extra code to the new brach")
    }
}

struct CoolBeanTestView_Previews: PreviewProvider {
    static var previews: some View {
        CoolBeanTestView()
    }
}
